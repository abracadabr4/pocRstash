input {
    redis {
        host => "redis-col"
        key => "myqueue"
        password => "pass"
        data_type => "list"
        codec => "plain"
    }
}

output {
#    stdout {
#        codec => rubydebug
#    }
    file {
        path => "/app/logs/output.log"
    }
}
